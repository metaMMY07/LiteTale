# Astra 交接：LiteTale 主工程迁移完成

交接日期：2026-09-11

后续唯一工作目录：`D:\Projects\LiteTale`

## 迁移审查结论

- C 盘旧工程路径：`C:\Users\30622\Documents\Codex\2026-08-31\x\work\wild-source`（工程内容已由用户于 2026-09-11 手动清空，空目录可能保留）
- D 盘主工程：`D:\Projects\LiteTale`
- C 盘工程中 Git 标记为已修改或新增的 60 个路径，全部能在 D 盘找到。
- 其中 58 个文件参与内容核对：48 个字节完全一致，10 个文件是 D 盘后续修正，不是迁移遗漏。
- 差异内容包括空白格式、正式版版本号与 `crypto` 依赖、字体许可证注释、异步异常保护、原生轻书架登录表单，以及补充的多书源回归测试；这些以 D 盘版本为准。
- 清空前 C 盘其余大量文件是 `.dart_tool`、`build` 和平台临时生成物，不属于源码迁移缺失；清空完成后 D 盘成为唯一工程副本。

## 当前状态

- Android 正式版 `0.0.18` 已构建并发布到 GitHub LiteTale 仓库。
- 默认分支和功能分支均已同步当前 LiteTale 版本。
- 文库8与轻书架按单一活动书源工作，账号、搜索、收藏和阅读历史隔离保存。
- 轻书架原生登录、专用字体、真实正文和插图已在 Android 15 x86_64 模拟器核验。
- 55 项本地测试通过，APK 签名、完整性和 16 KB 对齐检查通过。

## 后续工作规则

1. 进入 `D:\Projects\LiteTale` 后先检查 `git status` 和本文件。
2. 只在 D 盘主工程修改、构建和提交；C 盘旧工程内容已清空，不再作为工作副本或恢复来源。
3. 构建使用 `tools/build_android.ps1`，不要使用 C 盘旧目录中的脚本或生成物。
4. 不要创建或恢复 C 盘旧工程副本；所有新文件、构建缓存和交接记录都放在 D 盘。
5. 轻书架云书架同步、授权离线下载和文库8真实账号仍未接入或验证，新增功能需保持书源数据隔离。

## 版本和仓库

- GitHub：<https://github.com/metaMMY07/LiteTale>
- 正式版：<https://github.com/metaMMY07/LiteTale/releases/tag/v0.0.18>
- 主工程分支：`feat/litetale-multi-source`
- 默认分支：`main`
- 当前工作应以 D 盘工作区和远程仓库最新提交为准。
